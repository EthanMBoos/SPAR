"""Environment-family Blender helpers built on the shared Worldfile kernel.

Add a module only for deterministic logic that recurs within a developed
environment family. Shared mechanics remain in ``worldfile.blender_helpers``.
"""
