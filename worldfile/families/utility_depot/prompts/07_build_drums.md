# 07 — Build drums and the inspection target

Build every `barrel` and `inspection_target` anchor, and nothing else.

Create one semantic root per anchor. Each drum is a real-scale vertical
cylinder with simple local top and lower rings. Use varied muted blue, green,
and gray placeholder materials for ordinary barrels. The single
`inspection_target` root must retain `worldfile_role="anomaly"` and be the scene's
only saturated-red object. Do not add small final details yet.

Finish in Solid Material Color mode and save
`<REPO>/artifacts/worldgen/<WORLD>/07_drums.blend`.
