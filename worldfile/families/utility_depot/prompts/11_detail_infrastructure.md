# 11 — Detail infrastructure

Preserve every semantic root transform. Detail only roots of type `fence`,
`gate`, `structure`, `utility_cabinet`, and `rack`.

Add moderate local silhouette detail: chain-link or sparse mesh treatment,
gate bracing, shed roof structure, cabinet handles and vents, rack bracing,
and believable shelf loads. Parent every new component locally to its existing
root. Set `worldfile_no_collision=true` on thin decorative pieces that should not
affect motion or sensors. Keep interactive viewport performance and all aisles.

Finish in Solid Material Color mode and save
`<REPO>/artifacts/worldgen/<WORLD>/11_infrastructure_detail.blend`.
