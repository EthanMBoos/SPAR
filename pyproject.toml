[project]
name = "spar-host-tools"
version = "0.1.0"
description = "Host-side world checks and MuJoCo viewer for SPAR"
requires-python = ">=3.14,<3.15"
dependencies = [
  "mujoco==3.10.0",
  "pyyaml>=6.0,<7.0",
]

[tool.uv]
package = false
